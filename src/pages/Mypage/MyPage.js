const MyPage = () => {
    return (
        <div style={{display: 'grid'}}>
            <button onClick={"a"}>내 정보</button>,
            <button onClick={"a"}>작성 글 / 리뷰</button>,
            <button onClick={"a"}>공지 사항</button>,
            <button onClick={"a"}>관심 있는 술</button>,
            <button onClick={"a"}>북마크</button>,
            <button onClick={"a"}>임시저장 글</button>,
            <button onClick={"a"}>로그아웃</button>
        </div>
    );
}

export default MyPage;